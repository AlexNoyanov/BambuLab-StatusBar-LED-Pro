// #ifndef SOUND_ALERTING_H  // Prevents double inclusion
// #define SOUND_ALERTING_H

// class SoundAlerting {
//   public:
//     // Uses BUZZER_PIN from config.h if no pin specified
//     SoundAlerting(int pin = BUZZER_PIN);

//     SoundAlerting(int pin);  // Declaration only
//     void playAlert();        // Declaration only
// };

// #endif